{
  "name": "wowsergl",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace @wowsergl/editor run dev",
    "build": "npm --workspace @wowsergl/editor run build",
    "typecheck": "npm --workspace @wowsergl/editor run typecheck",
    "preview": "npm --workspace @wowsergl/editor run preview"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
